/**
 * Configuration Index
 *
 * Main export file for all configuration.
 *
 * Usage:
 * import { siteConfig, themeConfig } from '@/config';
 * import { doctorContent, teamMembers } from '@/config/content';
 */

export * from './site.config';
export * from './theme.config';
